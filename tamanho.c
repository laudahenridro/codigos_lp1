// Código que imprime o tamanho dos tipos em C
#include <stdio.h>

int main(){
	printf("%ld, %ld, %ld, %ld, %ld, %ld, %ld e %ld\n",sizeof(char),sizeof(short),sizeof(int),sizeof(long),sizeof(long long), sizeof(float),sizeof(double),sizeof(long double));
}
